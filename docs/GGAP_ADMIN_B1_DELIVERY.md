# 總後台 B1 第一批交付紀錄

日期：2026-09-07。範圍：9/5 基準上既有本地修改的延續，僅前端，不部署。

## 實作與驗收邊界

| 頁面／操作 | 本批狀態 | 後續缺口 |
| --- | --- | --- |
| 供應商／商戶／代理對帳匯出 | 共用 CSV，當前完整篩選結果與清單顯示欄位 | 正式後端授權、匯出稽核、大量資料與正式欄位契約 |
| 差異案件匯出 | 共用 CSV，保留目前依期限排序 | 精度政策仍待定；本批沿用畫面格式 |
| 帳號／角色／權限／資料範圍匯出 | 明確未開放 | 欄位、敏感資訊與授權契約 |
| 語言地區／時區、系統紀錄、幣別、匯率歷史匯出 | 明確未開放 | 各模式欄位與下載實作 |
| 新增角色／資料範圍／幣別、商戶批次操作 | 明確未開放 | 真正新增／批次流程及權限 |
| 商戶對帳重新計算 | 明確未開放，不再呼叫占位方法 | 核准公式、精度、快照與一致性驗證 |

本批 CSV 是人員核對用途，金融數值包含與畫面相同的幣別及格式，不是可直接匯入結算引擎的機器介面。正式登入及細粒度權限尚未接上；本批不增加新的資料來源或繞過路由限制。

## 測試

- 新增 `scripts/check-csv-export.ts`：中文、引號、換行、空值、負數精度、公式字串、欄數不符、模擬瀏覽器下載與釋放資源、頁面使用完整篩選結果及重算入口契約。
- 型別檢查通過；全專案 ESLint 共 451 個檔案，0 錯誤、0 警告；差異空白檢查通過（既有檔案有 Windows 換行提示）。
- 新增 CSV 測試與 8 組既有檢查通過：navigation、provider-demo、realignment、provider-core、provider-batch02、exchange-rate、finance-reconciliation、global-consistency。這些是靜態／本地模擬測試，不是正式服務整合證明。
- 模擬下載生命週期已驗證；尚未逐頁在真實瀏覽器下載並開啟檔案驗收。
- 最終本地 Vite 建置通過（40.68 秒）；預覽入口 `http://127.0.0.1:3017/ggapp/` 回應 HTTP 200。未推送或部署 GitHub。

## 尚未處理

既有其他頁面的獨立匯出尚未遷移本次共用工具；沒有宣稱全站 CSV 安全性已完成。全域表格／圓角一致性、正式權限、未決商務規格及服務串接仍是後續工作。

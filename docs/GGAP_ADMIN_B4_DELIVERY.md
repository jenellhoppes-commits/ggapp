# B4：明細表格、頁首布局與側欄鍵盤修正

日期：2026-09-07；本地前端修改，未推送或部署 GitHub。

## 已實作

- 儀錶板、代理詳細、商戶詳細、注單詳細、交易詳細、正式入口報表查詢、開發者中心共 7 個頁面檔案的 16 張表格接回 ArtTable。保留資料來源、欄位插槽、操作、原分頁及專用空資料訊息；不強制邊框或尺寸，使用自然高度。原始報表舊頁與已停用頁面不因此恢復入口。
- 這 7 個檔案的固定像素圓角改用既有全域變數；未更動業務 script（逐檔比對忽略排版空白）。
- 共用 AppPageHeader 允許操作區換行，文字區可縮小並折行；行動版直向布局清除桌面寬度基準，避免長名稱、識別碼或操作列擠出容器。影響使用此組件的頁面，仍需實際畫面驗收。
- 移除儀錶板上方獨立演示資訊列及其 CSS，落實先前要求；不刪除資料版本、時區或資料模型。平台狀態仍呈現資料截止時間及版本。
- 行動版側欄焦點循環排除不可見、停用及 tabindex 負值項目，避免首尾循環落入已收起的子選單；不更動路由或群組展開流程。

## 驗收範圍

共用表格契約累計涵蓋 20 個檔案、42 張表格宣告。新增頁首換行、儀錶板資訊列移除與側欄焦點篩選的原始碼契約；既有金融、供應商、導覽及 CSV 檢查維持。

本輪透過瀏覽器讀取到本地儀錶板及導覽結構。但目前可用介面未提供畫面截取、點擊、鍵盤及調整視窗操作，因此不能宣稱深淺主題、收合、窄螢幕、固定欄位對齊與鍵盤循環已通過真實瀏覽器驗收。這些仍列為待驗收，而非以 HTTP 成功或靜態測試替代。

11 組靜態／本地模擬檢查、型別檢查與本地建置通過（最終建置約 62 秒）；預覽 HTTP 200，差異空白檢查通過。不是正式後端整合測試。

最終 ESLint 共 454 個檔案，0 錯誤、0 警告。

## 待辦

- 真實瀏覽器逐頁驗收上述介面情境。
- 其餘遊戲／供應商業務子元件等表格與特殊配置仍需盤點，不將未開放歷史頁面直接算為交付功能。
- B1 未開放功能、財務精度政策、正式權限與服務串接不在本批實作範圍。
